{
  "name": "@anytomarkdown/worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for AnyToMarkdown",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "author": "AnyToMarkdown Contributors",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240312.0",
    "typescript": "^5.0.0",
    "vitest": "^0.34.0",
    "wrangler": "^3.114.2"
  }
}
